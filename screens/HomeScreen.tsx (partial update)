// Add this somewhere in your JSX, perhaps at the top of the screen
<TouchableOpacity 
  style={{
    position: 'absolute',
    top: 10,
    right: 10,
    backgroundColor: theme.colors.error,
    padding: 10,
    borderRadius: 8,
    zIndex: 999,
  }}
  onPress={() => {
    console.log('Direct logout button pressed');
    onLogout();
  }}
>
  <Text style={{ color: 'white', fontWeight: 'bold' }}>Sign Out</Text>
</TouchableOpacity>